{
  "name": "sdk-node",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel --presets es2015 src -d dist && git add dist",
    "postinstall": "npm install @firebase/app && npm install @firebase/auth && npm install @firebase/firestore"
  },
  "repository": {
    "type": "git",
    "url": "git+https://gitlab.com/Tokium/sdk-node.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://gitlab.com/Tokium/sdk-node/issues"
  },
  "homepage": "https://gitlab.com/Tokium/sdk-node#README",
  "dependencies": {
    "@firebase/app": "^0.3.3",
    "@firebase/auth": "^0.7.4",
    "@firebase/firestore": "^0.7.2",
    "es6-promise": "^4.2.5",
    "events": "^2.0.0",
    "fetch-everywhere": "^1.0.5"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "pre-commit": "^1.2.2"
  },
  "pre-commit": [
    "build"
  ]
}
